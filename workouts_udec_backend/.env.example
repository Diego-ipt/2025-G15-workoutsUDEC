# Backend Environment Configuration

# Security
SECRET_KEY=your-secret-key-change-in-production-workouts-udec-app
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Database Configuration (matching docker-compose.yml)
POSTGRES_SERVER=db
POSTGRES_USER=workouts_user
POSTGRES_PASSWORD=workouts_password
POSTGRES_DB=workouts_db

# Optional: Override complete database URL
# DATABASE_URL=postgresql://workouts_user:workouts_password@db/workouts_db